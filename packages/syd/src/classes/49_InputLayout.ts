import { InputLayoutElement } from './46_InputLayoutElement';

export class InputLayout {
  elements: InputLayoutElement[];

  constructor(elements: InputLayoutElement[]) {
    this.elements = elements;
  }
}
